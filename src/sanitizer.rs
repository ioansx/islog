pub fn sanitize_new_content(lines: Vec<String>) -> Vec<String> {
    // Trim empty lines at the beginning

    // let mut invalid_incices: Vec<usize> = vec![];
    //
    // let mut start = lines.iter().enumerate();
    // while let Some((pos, line)) = start.next() {
    //     if line.trim().is_empty() {
    //         invalid_incices.push(pos);
    //     } else {
    //         break;
    //     }
    // }
    //
    // let mut end = lines.iter().rev().enumerate();
    // while let Some((pos, line)) = end.next() {
    //     if line.trim().is_empty() {
    //         invalid_incices.push(pos);
    //     } else {
    //         break;
    //     }
    // }

    lines
}
